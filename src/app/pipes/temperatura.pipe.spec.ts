import { TemperaturaPipe } from './temperatura.pipe';

describe('TemperaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturaPipe();
    expect(pipe).toBeTruthy();
  });
});

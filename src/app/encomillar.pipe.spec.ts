import { EncomillarPipe } from './encomillar.pipe';

describe('EncomillarPipe', () => {
  it('create an instance', () => {
    const pipe = new EncomillarPipe();
    expect(pipe).toBeTruthy();
  });
});
